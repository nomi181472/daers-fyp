export enum Subjects{
  UserCreated = "user:created",
  generateDietSchedule = "generate:dietschedule",
  UserUpdated="eschedule:updated",
  UserInformation="user:information"
}

const printSubject = (subject: Subjects) => {
   
  
}